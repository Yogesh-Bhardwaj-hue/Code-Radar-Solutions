#include <stdio.h>
#include <ctype.h>
int main(){
    char a;
    scanf("%c",&a);
    if(issupper(c)){
        printf("Uppercase");
    }
    else if(islower(c)){
        printf("Lowercase");
    }
    return 0;
    
}