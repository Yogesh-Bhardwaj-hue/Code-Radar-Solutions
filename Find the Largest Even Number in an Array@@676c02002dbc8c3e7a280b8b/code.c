#include <stdio.h>
int main(){
    int arr[n];
    int n;
    scanf("%d",&n);
    for(int i = 0 ; i<=n ; i++ ){
        scanf("%d",arr[n]);
        printf("%d",arr[n]);

    }
    return 0 ;
}